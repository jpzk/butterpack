cmake_minimum_required(VERSION 2.8)
project( butterpack )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( butterencode butterpack.cpp )
target_link_libraries( butterencode ${OpenCV_LIBS} )
